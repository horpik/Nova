<UserControl x:Class="Nova.Views.DataEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Nova.Views"
             xmlns:components="clr-namespace:Nova.Components"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <TextBlock Text="Ввод данных"></TextBlock>
            <ListBox SelectionMode="Multiple" ItemsSource="{Binding TagDataList}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding DescriptionTag}"></TextBlock>
                            <TextBox Text="{Binding TagValue}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="Ok" Command="{Binding NavigateHomeCommand}"></Button>
        </StackPanel>
    </Grid>
</UserControl>